/**
 * Пути структурных паттернов.
 * */
export class StructuralPaths {
    MAIN_STRUCTURAL = '/structural';
    ADAPTER = this.MAIN_STRUCTURAL + '/adapter';
    BRIDGE = this.MAIN_STRUCTURAL + '/bridge';
    COMPOSITE = this.MAIN_STRUCTURAL + '/composite';
    DECORATOR = this.MAIN_STRUCTURAL + '/decorator';
    FACADE = this.MAIN_STRUCTURAL + '/facade';
    FLYWEIGHT = this.MAIN_STRUCTURAL + '/flyweight';
    PROXY = this.MAIN_STRUCTURAL + '/proxy';
}
